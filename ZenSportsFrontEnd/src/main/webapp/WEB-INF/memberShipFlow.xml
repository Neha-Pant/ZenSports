<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow
                          http://www.springframework.org/schema/webflow/spring-webflow.xsd">

	<!-- Executed when webFlow is triggered -->

	<on-start>
		<evaluate expression="registerHandler.initializeModel()" result="flowScope.registerModel" />
	</on-start>

	<view-state id="start" view="registration/register" model="flowScope.registerModel">
		<transition on="submit" to="validateUser" />
	</view-state>

	<!-- if not specified the first view state is the starting state -->

	<action-state id="validateUser">
		<evaluate expression="registerHandler.validateUserDetails(registerModel.user)"></evaluate>
		<transition on="success" to="billing" />
		<transition on="failure" to="start" />
	</action-state>

	<view-state id="billing" view="registration/billing" model="flowScope.registerModel">
		<transition on="submit" to="validateAddress" />
	</view-state>

	<action-state id="validateAddress">
		<evaluate expression="registerHandler.validateAddressDetails(registerModel.address)"></evaluate>
		<transition on="success" to="confirm" />
		<transition on="failure" to="billing" />
	</action-state>

	<view-state id="confirm" view="registration/preRegister" model="flowScope.registerModel">
		<transition on="edit" to="start" />
		<transition on="submit" to="welcome" />
	</view-state>

	<end-state id="welcome" view="registration/welcome" />
	<end-state id="toHome" view="externalRedirect:contextRelative:home.jsp" />
	<end-state id="toException"
		view="externalRedirect:contextRelative:exception.jsp" />

	<!-- these events are available in every state -->
	<global-transitions>
		<transition on="home" to="toHome" />
		<transition on="error" to="toException" />
	</global-transitions>
</flow>